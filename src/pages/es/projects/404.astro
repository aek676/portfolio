---
import Cube3D from "@/components/Cube3D.astro";
import Layout from "@/layouts/Layout.astro";

const base = import.meta.env.BASE_URL;
---

<Layout title="404 - Anass">
  <section
    class="h-screen flex flex-col justify-center items-center text-center px-4 relative"
  >
    <div class="max-w-4xl mx-auto">
      <h1
        class="text-6xl md:text-8xl font-black mb-6 tracking-tighter leading-none text-gradient" 
      >
        404
      </h1>

      <h2 class="text-3xl md:text-4xl font-bold mb-8 text-(--color-text-main)">
        aquí no era
      </h2>
      <div class="flex flex-col items-center gap-8 mb-12">
        <Cube3D />
      </div>

      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a
          href={`${base}/`}
          class="px-8 py-3 bg-emerald-500 text-black font-bold rounded-full hover:bg-emerald-400 transition-all duration-300 shadow-lg shadow-emerald-500/20"
        >
          Volver al inicio
        </a>
      </div>
    </div>
  </section>
</Layout>

